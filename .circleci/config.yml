version: 2.1

orbs:
  python: circleci/python@2.0.3

jobs:
  build:
    executor: python/3.9.10
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: pip
          app-dir: ~/project/DjangoWebProject
      - run:
          name: Test
          working-dir: ~/project/DjangoWebProject
          command: |
            python --version
workflows:
  main:
    jobs:
      - build